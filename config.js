/* ============================================================
   ViralFood / Chinaglia S.R.L. — Lead capture configuration
   ------------------------------------------------------------
   Paste the Web App URL you get after deploying google-apps-script.gs
   (Apps Script → Deploy → New deployment → Web app → copy the /exec URL).

   Until you fill this in, the form gracefully falls back to opening
   the visitor's email app addressed to info@chinagliafederico.com.
   ============================================================ */

window.LEAD_ENDPOINT = ""; // e.g. "https://script.google.com/macros/s/AKfycb..../exec"
